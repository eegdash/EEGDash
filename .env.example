# EEGDash Environment Variables
# Copy this file and fill in your values:
#   cp .env.example .env
#   Then edit .env with your actual credentials

# =============================================================================
# EEGDash API (Required for injection)
# =============================================================================
# Admin token for write operations (upsert, delete, compute-stats)
# Find the token in: mongodb-eegdash-server/docker-compose.yml (ADMIN_TOKEN)
# Run: grep ADMIN_TOKEN mongodb-eegdash-server/docker-compose.yml
EEGDASH_ADMIN_TOKEN=AdminWrite2025SecureToken

# API URL (default: https://data.eegdash.org)
# EEGDASH_API_URL=https://data.eegdash.org

# =============================================================================
# External Data Sources (Optional - for fetching datasets)
# =============================================================================

# Zenodo API key - Get from: https://zenodo.org/account/settings/applications/tokens/new/
# Provides 100 req/min (vs 60 req/min for anonymous)
# ZENODO_API_KEY=your-zenodo-api-key

# Figshare API key - Get from: https://figshare.com/account/applications
# FIGSHARE_API_KEY=your-figshare-api-key

# NEMAR CLI API key
# NEMAR_API_KEY=your-nemar-api-key

# =============================================================================
# GitHub (Optional - for fetching dataset info from GitHub)
# =============================================================================
# GitHub Personal Access Token - Get from: https://github.com/settings/tokens
# GITHUB_TOKEN=your-github-token
